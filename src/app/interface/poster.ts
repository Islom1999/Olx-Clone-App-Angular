export type PosterRequest = {
    fulName: string;
    email: string;
    phone: string;
    region: string;
    password: string;
}

export type PosterResponse = {
    fulName: string;
    email: string;
    phone: string;
    region: string;
    password: string;
}